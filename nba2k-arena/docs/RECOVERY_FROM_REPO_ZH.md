# 从仓库历史可见版本重新开发

用户最新要求是以 repo 里能玩的版本为底。011 是球队场地，700 是新手教学场地，blacktop 是街球场地；用户测试正常的 011 使用原来的 011 文件名。这次不以 011 替代项目基底，不改变 700 文件名。

## 选定基底

`fde9489` 中 `output/checkpoint1/line_fix/` 的两份 IFF。用户历史图 `evidence/user_game_screenshot_checkpoint1_REJECTED.png` 显示这版的球员、篮筐、地板和建筑可见，标线较 V2 变细、额外四分弧消失。它仍有室内环境、青色边框、黑色棕榈树、油亮地板和缺失中圈等问题，只是恢复开发的起点。

原样恢复包：`output/recovery_base/repo_checkpoint1_restore_700.zip`。两份 IFF 通过 `git show` 直接提取，其内部没有再次封装或修改；本地文件与分发包内容均直接比较字节，等于 Git 中归档。没有计算文件哈希。

| 文件 | 字节 |
| --- | ---: |
| arena_700_int.iff | 48,674,919 |
| arena_700_int_floor.iff | 445,558 |

本轮又通过本机 Discord 观察到了旧场地：有颜色的球员、灰色地面、白色标线和青色边框重新出现；用户窗口中也显示了本恢复包的传输记录。中央窗口仍遮住部分画面，尚未完整观察移动和投篮。该观察支持恢复显示，但不等于追踪了游戏实际加载的每份文件，也不能把恢复包说成已经完成的室外球场。

## 第一项资产迁回已获用户确认

`output/recovery_floor_step1/repo_base_floor_step1_700.zip`（34,271,790 字节），主 IFF 60,722,949 字节，配套 floor 445,558 字节。用户实测回复：“地板是正常的，下一步”。Discord 中看到灰色水泥纹理、新罚球虚线、正常有色球员和篮架，并出现 7.17 米投篮反馈。这一地板检查点成立，后续保留它；整个室外场景仍未完成。

- 输入仍直接来自 Git 的 Checkpoint 1，后续失败 IFF 不参与场景构建。
- 只迁入 R4 的 74 段球场几何，包括中线、中圈和标准标线，共 28,041 顶点；保留旧版球场边界、变换和顶点格式。
- 原地板材质只改灰色水泥的颜色贴图；两个新增标线材质均复制旧版原生线材质，仅换颜色贴图。
- 所有原 IFF 成员中仅 `level.SCNE` 变化，另外增加 2 个现成几何二进制、3 张现成颜色贴图；其他成员与 Git 基底逐字节相同。配套 floor 原样复制。
- 灯光、曝光、后处理、地板光照、篮筐、玩法锚点，以及原法线和反射配置全部保留。没有迁入 R5 浅色禁区覆盖层。

构建：`python3 nba2k-arena/scripts/build_recovery_floor.py`。该脚本只读已有 Git/ZIP 资产，不调用旧构建链，不重算文件哈希或索引校验值。结构检查只保护改动范围；本次实际反馈单独记在 `output/recovery_floor_step1/runtime_feedback.json`，不会让构建脚本自动宣称任意未来输出已实测。

## 已被否定的路线

R6 的人物与场地严重过曝；R7、R7.1、R7.2、R7.3 均收到用户黑屏反馈。R7.3 的 DDS 转换没有解决黑屏。不要再把静态结构检查通过写成游戏修好了，不要从这些失败场景继续累积参数。

## 资产迁回次序

1. 原样恢复上述仓库基底，确认当前安装仍能看到人物、地板与篮筐。
2. 基于同一份 Git 归档制作局部地板候选：复用 R4 中已经生成的标准标线网格及灰色水泥颜色贴图。新增标线材质从基底本身复制，只接入所需颜色贴图。保留基底的灯光、曝光、后处理、光照贴图和全部玩法记录。
3. 看到中圈和球员均正常后，再单独处理反光、室内装饰移除和室外资产；每次实际确认显示，避免同时改变照明、材质和场景结构后无法辨别是哪一步坏掉。

局部地板候选保留旧版反射贴图，不声称解决油光。后续树木、台阶、金属看台、地形和建筑资产可以单独复用，材质及光照绑定需在基底上逐项接入。不得整体复制 R7 的 Light、Object.UserData 或静态光照配置。

不安装建模软件，不写入用户游戏目录，不操作直播中的 Windows 电脑。用户已授权正常版本完成后直接推送，不重复询问；当前局部候选未实际确认，不能冒充完成版推送。
