# R7.1 曝光复原：实测仍然全黑

用户的要求是：修好故障，交付能在 NBA 2K 中正常显示、正常打球的球场，做完可以直接推送。用户随后在本机 Discord 打开了游戏电脑的直播；代理可以观察画面，**不能控制那台电脑**。替换文件和操作游戏由用户完成。未确认正常的调整包只在本地提供，不提前推送为正式版本。

已实际观察直播中的投篮提示、脚下玩家标识变化，但三维场景仍全黑。用户随后提供实际安装文件的属性截图：`arena_700_int.iff` 大小为 **195,512,151 字节**，与 R7.1 完全一致，位置以 `D:\steam\steamapps\common\NBA 2K26\mods\` 开头，尾部被属性窗口截断。R7.1 的实测结果是仍然全黑，不能继续要求用户重装同一包，也不能声称仅恢复曝光就解决了问题。

R7 的游戏画面全黑，界面和脚下标识仍然可见。R7 同时修改了曝光、灯光和场外光照绑定，无法凭单张截图断定哪个改动造成了全部问题。新一轮首先只撤回没有实机依据的曝光修改。

| 字段 | R7 | R7.1（直接取自 R6） |
| --- | --- | --- |
| 自动曝光开关 | 关闭 | 开启 |
| 保存的 EV100 | 15 | 10.1999998 |

R6 截图中的天空有正常蓝色，但球场仍有过曝与黑色场外物体，所以这份配置只提供一个已有画面依据的起点，不能称为正常球场的最终配置。

本次不改变任何灯、材质、网格、贴图、球场标线、篮筐位置、碰撞或玩法数据。R7 的灯光关闭状态和场外材质绑定保留，其运行效果仍待确认。

本地包：`output/revision7_1/venice_court_revision7_1.zip`，约 98 MB，包含原 slot 700 的两份配套 IFF。解压外层 ZIP，保留旧版备份后一起替换，完全退出游戏再进入球场，提供与上一张相近机位的截图。

下一份本地调整为 [R7.2](REVISION7_2_ZH.md)。只有正常显示且玩法正常后，才能把新的球场版本推送到仓库。现有脚本检查仅确认这次没有顺带改动其他内容，不能代替实际游戏结果。

本地记录：7 项定向检查通过；构建直接比较确认只有 `level.SCNE` 改变，里面只有两个曝光字段改变，配套 floor IFF 保留。未计算文件哈希，未写入游戏目录，未执行游戏。
