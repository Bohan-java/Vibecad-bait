# R7.2 原主灯复原：实测仍然全黑

R7.1 恢复自动曝光后仍全黑；用户提供的安装文件大小与交付一致。本次恢复原 donor 的整套四盏主灯记录和原本关闭的 Sun，保留 R6/R7.1 天空与曝光配置，恢复被 R7 清零的地面、场外物体灯光可见性。

这份包保留 R7 场外材质和静态光照绑定。用户反馈没有任何变化，代理也在 Discord 直播中看到三维场景仍黑。恢复曝光、主灯和受光配置没有解决问题，不得宣称已修复，也不要要求用户重复安装这一包。

用户随后解释：他们替换球场及地板文件进行对照，11 号球场正常显示。因此直播中 levels 文件夹没有 700 文件，是对照过程，不能当作用户放错文件的证据。用户准备提供正常的 11 号球场和地板 IFF。收到后应以实际能显示的文件为依据，对照语法以外的游戏结构约束、材质绑定和光照参数，继续排查。暂不发布新的猜测性曝光版本。

本地文件：`output/revision7_2/venice_court_revision7_2.zip`，102,275,606 字节。内含：

| 文件 | 大小（字节） |
| --- | ---: |
| arena_700_int.iff | 195,513,528 |
| arena_700_int_floor.iff | 445,558 |

以上包保留作失败对照，不推荐继续安装。代理不能控制另一台电脑，所有游戏操作由用户完成。未确认可用之前，调整包只在本地交付，不推送为正式版本。

本地已完成 7 项定向检查。四盏主灯、Sun 与原 donor 对应记录直接相等；所有模型、材质、对象、效果记录保持原样；旧二进制资源逐字节保持原样；只修改两个 SCNE 并新增一份 352 字节灯光可见性贴图。配套 floor IFF 未变。未计算文件哈希，未安装软件，未写入游戏目录。
